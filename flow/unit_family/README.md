# Unit Family Flows

- `ground_light_tank_flow.md`
  - why `light tank` is currently missing
  - current runtime path
  - negative findings for `class_id` and `unit_ptr + 0x98`
  - active pragmatic path: dynamic `unit_key/profile_path` pattern resolver
  - next reverse targets only after pattern path is exhausted
